% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_marked_mscjs_multiple.R
\name{fit_marked_mscjs_multiple}
\alias{fit_marked_mscjs_multiple}
\title{Fit a Multistate CJS Model Using \code{marked}}
\usage{
fit_marked_mscjs_multiple(
  ms_data,
  site_mapping,
  s_formula = ~time * release_group,
  p_formula = ~stratum * time * detect_site,
  psi_formula = ~-1 + stratum:tostratum,
  hessian = TRUE,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{ms_data}{Data frame from \code{build_multistate_histories()} with
\code{tag_code}, \code{ch}, and optional covariates.}

\item{site_mapping}{Data frame with \code{release_group}, \code{detect_site}, and \code{occ}.}

\item{s_formula}{Formula for survival. Default \code{~ time}.}

\item{p_formula}{Formula for detection. Default \code{~ time}.}

\item{psi_formula}{Formula for transition probabilities. Default
\code{~ -1 + stratum:tostratum}.}

\item{hessian}{Logical; passed to \code{marked::crm()}.}

\item{conf_level}{Confidence level for intervals.}

\item{...}{Additional arguments passed to \code{marked::crm()}.}
}
\value{
List with \code{model}, \code{phi}, \code{cum_phi}, \code{p},
\code{psi}, and \code{covariance_mode}.
}
\description{
Fits a multistate CJS model for multiple release groups using \code{marked::crm()} with
\code{model = "hmmMSCJS"}. Designed for encounter histories where censored or
removed fish are represented as a separate absorbing state. Survival is estimated separately
for each release group, and detection probability is estimated jointly.
}
\author{
Ryan N. Kinzer

Michelle A. Briggs
}
