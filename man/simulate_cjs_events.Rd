% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cjs_events.R
\name{simulate_cjs_events}
\alias{simulate_cjs_events}
\title{Simulate PIT-tag CJS Data with Removals and Added Releases}
\usage{
simulate_cjs_events(
  n_init = 1000,
  n_add = NULL,
  sites = paste0("site_", 1:5),
  S = rep(0.8, length(sites) - 1),
  p = c(1, rep(0.5, length(sites) - 1)),
  remove_prob = rep(0, length(sites)),
  start_date = as.POSIXct("2025-04-01", tz = "UTC"),
  seed = NULL
)
}
\arguments{
\item{n_init}{Number of fish released at the first site.}

\item{n_add}{Optional integer vector of new releases added at each site.
Length must equal number of sites. Usually first element should be 0.}

\item{sites}{Character vector of ordered site names.}

\item{S}{Survival probabilities between sites. Length must be length(sites) - 1.}

\item{p}{Detection probabilities at sites. Length must equal length(sites).
The first value is ignored if the first site is release-only.}

\item{remove_prob}{Probability of removal after detection at each site.
Length must equal length(sites).}

\item{start_date}{Starting date-time for simulated observations.}

\item{seed}{Optional random seed.}
}
\value{
Data frame with tag-level event records.
}
\description{
Simulates downstream detection histories for testing CJS, multistate CJS,
and M-array workflows. Fish can survive between sites, be detected at sites,
be removed after detection, and new marked fish can be added at downstream sites.
}
\author{
Ryan N. Kinzer
}
